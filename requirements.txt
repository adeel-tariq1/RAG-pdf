# FIRST, explicitly require the newer versions that Streamlit needs
# This forces pip to prioritize these versions and not downgrade them.
rich>=14.1.0
markdown-it-py>=4.0.0
pygments>=2.19.2
mdurl>=0.1.2

# CORE APP
streamlit==1.30.0

# LANGCHAIN & DEPS: Use modern, compatible versions.
# Avoid the ancient v0.1.0 which is full of conflicts.
langchain==0.1.16 # A much newer, stable 0.1.x release
langchain-community==0.0.34
langchain-core==0.1.45

# LangChain Integrations - use compatible ranges
langchain-groq>=0.1.0
langchain-openai>=0.1.0
langchain-google-genai>=0.1.0

# EMBEDDINGS & VECTORSTORE
sentence-transformers>=2.2.2
chromadb==0.4.24 # Pinning a specific version of ChromaDB is often safe

# DOCUMENT PROCESSING
pypdf==3.15.0

# CORE ML
torch>=2.0.0
numpy>=1.24.0
